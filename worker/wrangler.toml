name = "mlb-live"
main = "dist/worker.mjs"
compatibility_date = "2026-09-01"
# dist/worker.mjs is already one self-contained module.
no_bundle = true

[observability]
enabled = true

# Optional: `npx wrangler secret put CONNECTOR_KEY` moves the endpoint to /mcp/<key>.
